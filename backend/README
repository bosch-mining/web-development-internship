To instantiate the database it is necessary to use Docker.
There is a file called docker-compose.yml that is necessary to start the bank, just use the command "docker-compose up" at the root of the project for this.
The PostgreSQL database was used.
