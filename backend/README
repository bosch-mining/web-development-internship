Para instanciar o banco de dados é necessario o uso do Docker.
Existe um arquivo chamado "docker-compose" que é necessario para iniciar o banco, basta utilizar o comando "docker-compose up" na raiz do projeto para isso.
Foi utilizado o banco de dados PostgreSQL.
